Author: Emily Freund

Task 1: Part C: 
--Update pip and install pandas sci-kit-learn openpyxl 
-- import pandas and load the excel template DQN1-Dataset.xlsx 
--view the PM2.5 and CO2 columns 
--set the target column "healthRiskScore" 
--set the variables x,y --print and check

--split the data into training and testing sets and import the DecisionTreeRegressor 
--create the model and fit onto the training data

Part D:

-- import mean_squared_error, r2_score, numpy --run prediction on the test set --print the results to console

Task 2: Part B 
--apply optimization 
--import GridSearchCV 
--create the grid parameters 
--add grid search --fit the grid search to the training data

--import RandomizedSearchCV, randint, mean_squared_error, r2_score
--create the hyperparameter split distributions
--create random search and fit tot he training data

--apply regularization 
--add the limiting tree depth code 
--add the minimum samples per leaf code 
--apply ensemble learning techniques
--import the randomforestregressor & gradientboostingregressor
-- create tne regression averages and fit the data to the model Part C 
--apply evaluation metrics 
-- use the same evaluation metrics 
-- mean_squared_error, r2_score 
--run prediction on the test set 
--print the results to console 
--summarize printed results for each model / optimization technique
